name: example
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.3

# Add regular dependencies here.
dependencies:
  # json_annotation: ^4.8.1
  gclass_annotation:
    path: ../gclass_annotation
    

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
  # json_serializable: ^6.7.1
  build_runner: ^2.4.7
  build_test: any
  gclass:
    path: ../gclass
  
